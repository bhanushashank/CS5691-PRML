Instructions for executing the code files:
- Place the required dataset directories "Isolated_Digits", "Handwriting_Data", "HMM-Code", "Features" in the folder containing the python scripts (Code).
- The above directories can be placed as it is (as they are available in google drive)
-Run the python scripts to receive plots and other outputs.